<UserControl x:Class="WindowsPet.Views.Ucontrol.DashBoardCardUserPet"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsPet.Views.Ucontrol"
             xmlns:vm="clr-namespace:WindowsPet.VM"
             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock Style="{StaticResource TextblockStyle}" Text="{Binding DashBoardName, Mode=TwoWay,RelativeSource={RelativeSource AncestorType=local:DashBoardCardUserPet}}" HorizontalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="1">
            <ScrollViewer Style="{StaticResource CustomScrollViewer}" >
                <ItemsControl  ItemsSource="{Binding OnlinePets,RelativeSource={RelativeSource AncestorType=local:DashBoardCardUserPet}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:UserPet DataContext="{Binding}" PetImage="{Binding Image}" PetName="{Binding Name}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
    
</UserControl>
